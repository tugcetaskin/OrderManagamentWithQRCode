@model List<SignalRWebUI.DTOs.ProductDTOs.ResultProductWitCategoryDTO>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
    int count = 0;
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Ürün Listesi</h4>
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th> Id </th>
                            <th> Resim </th>
                            <th> Ürün Adı </th>
                            <th> Kategori </th>
                            <th> Fiyat </th>
                            <th> Durum </th>
                            <th> Güncelle </th>
                            <th> Sil </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach(var item in Model)
                        {
                            count++;
                            <tr>
                                <td> @count </td>
                                <td class="py-1">
                                    <img src="@item.ImageUrl" alt="image" />
                                </td>
                                <td> @item.Name </td>
                                <td> @item.CategoryName </td>
                                <td> @item.Price </td>
                                <td> @item.Status </td>
                                <td> <a href="/Product/Update/@item.Id" class="btn btn-success"><i class="mdi mdi-refresh"></i></a> </td>
                                <td><a href="/Product/Delete/@item.Id" class="badge badge-danger"><i class="mdi mdi-delete"></i></a></td>
                            </tr>
                        }
                    </tbody>
                </table>
                <a href="/Product/Create" class="badge badge-info">Ekle</a>
            </div>
        </div>
    </div>
</div>

